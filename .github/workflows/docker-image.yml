name: Build and Push Docker image

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Grant execute permission for Maven Wrapper
        run: chmod +x ./mvnw

      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          distribution: 'temurin'  # This specifies the Java distribution to use
          java-version: '11'

      - name: Build with Maven
        run: ./mvnw clean package

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

#      - name: Build Docker image
#        run: docker build -t mirfanduri/springboot-slack:latest .

      - name: Pull Docker image
        run: docker pull mirfanduri/springboot-slack:latest

#      - name: Push Docker image to Docker Hub
#        run: docker push mirfanduri/springboot-slack:latest
